services:
  discord-bot:
    container_name: discord-bot
    image: ghcr.io/madelponte/discord-bot:latest
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - ainet

networks:
  ainet:
    external: true
    name: ainet
